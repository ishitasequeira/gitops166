config:
  argocd:
    namespace: argocd
  pipelines:
    name: july-cicd
environments:
- apps:
  - name: app-taxi
    services:
    - taxi
  name: july-dev
  pipelines:
    integration:
      bindings:
      - github-push-binding
      template: app-ci-template
  services:
  - name: taxi
    pipelines:
      integration:
        bindings:
        - july-dev-taxi-binding
        - github-push-binding
    source_url: https://github.com/ishitasequeira/taxi.git
    webhook:
      secret:
        name: webhook-secret-july-dev-taxi
        namespace: july-cicd
- name: july-stage
gitops_url: https://github.com/ishitasequeira/gitops166.git
